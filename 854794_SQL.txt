


1)Write a query to displayy department_name,no_of_employees with departments having 20 or more employees.

select department_name, count(employee_id) as mo_of_employees from employees e join departments d on
e.department_id=d.department_id
group by department_name
having count(employee_id)>20;

2)SELECT EMPLOYEE_ID,FIRST_NAME,LAST_NAME,SALARY FROM EMPLOYEES A
WHERE SALARY>(SELECT AVG(SALARY)FROM EMPLOYEES B WHERE A.DEPARTMENT_ID=B.DEPARTMENT_ID 
GROUP BY A.DEPARTMENT_ID);

3)
SELECT EMPLOYEE_ID,FIRST_NAME,LAST_NAME,(FIRST_NAME||''||LAST_NAME)FULL_NAME,SALARY,(SALARY*0.1)NEW_SALARY,(SALARY-(SALARY*0.1))DIFFERENCE FROM EMPLOYEES;


4)SELECT A.EMPLOYEE_ID,A.FIRST_NAME,A.LAST_NAME,B.FIRST_NAME AS MANAGER_NAME
FROM EMPLOYEES A LEFT OUTER JOIN EMPLOYEES B ON A.MANAGER_ID = B.MANAGER_ID ORDER BY EMPLOYEE_ID;

5)SELECT EMPLOYEE_ID,FIRST_NAME,SALARY
FROM (SELECT *FROM EMPLOYEES ORDER BY SALARY DESC)
WHERE ROWNUM<=3 ORDER BY SALARY;


THANKS AND REGARDS
ANWESH