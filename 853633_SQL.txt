ANSWER FOR Q1

SELECT DEPARTMENT_NAME, COUNT(EMPLOYEE_ID) AS NO_OF_EMPLOYEES 
FROM DEPARTMENTS D
INNER JOIN EMPLOYEES E
ON (E.DEPARTMENT_ID=D.DEPARTMENT_ID)
GROUP BY DEPARTMENT_NAME
HAVING COUNT(EMPLOYEE_ID) >= 20;


ANSWER FOR Q2

SELECT  EMPLOYEE_ID, FIRST_NAME, lAST_NAME, SALARY FROM EMPLOYEES A
WHERE SALARY >(SELECT AVG(SALARY) FROM EMPLOYEES B
WHERE A.DEPARTMENT_ID=B.DEPARTMENT_ID
GROUP BY A.EMPLOYEE_ID);




ANSWER FOR Q3.

select employee_id,first_name, last_name, first_name ||' '|| last_name AS fullname, salary, (salary+(salary/10)) as new_salary,
((salary+(salary/10)) - salary) as DIFFERANCE
from employees;




ANSWER FOR Q5

SELECT EMPLOYEE_ID,FIRST_NAME, SALARY 
FROM EMPLOYEES
WHERE ROWNUM <=3 
ORDER BY SALARY DESC;


